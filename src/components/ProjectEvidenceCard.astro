---
interface Props {
  title: string;
  mechanism: string;
  problem: string;
  boundary: string;
  outcome: string;
  constraints: string;
  href?: string;
}

const {
  title,
  mechanism,
  problem,
  boundary,
  outcome,
  constraints,
  href,
} = Astro.props;
---

<article class="project-evidence-card u-card-overflow-safe">
  <div class="mini-trace" aria-hidden="true">
    <span>Pressure</span>
    <span>Boundary</span>
    <span>Mechanism</span>
    <span>Outcome</span>
  </div>
  <h3 class="u-break-anywhere">{title}</h3>
  <p class="outcome-signal u-break-anywhere">{outcome}</p>
  <dl>
    <div>
      <dt>Problem</dt>
      <dd class="u-break-anywhere">{problem}</dd>
    </div>
    <div>
      <dt>Decision boundary</dt>
      <dd class="u-break-anywhere">{boundary}</dd>
    </div>
    <div>
      <dt>Mechanism</dt>
      <dd class="u-break-anywhere">{mechanism}</dd>
    </div>
    <div class="constraints">
      <dt>Constraint</dt>
      <dd class="u-break-anywhere">{constraints}</dd>
    </div>
  </dl>
  {href && (
    <p>
      <a class="inline-link" href={href}>Open case study</a>
    </p>
  )}
</article>
